imports:
    - { resource: parameters_test.yml }

# Framework Configuration
framework:
    test: ~
    secret: DontTellThisToAnyone

fox_elasticsearch:
    clients:
        default:
            host: 127.0.0.1
            port: 9200
    indexes:
        foo:
            index_name: foo_index
            default: true
            settings:
                refresh_interval: -1
                number_of_replicas: 1
        bar:
            index_name: bar_index
            settings:
                refresh_interval: -1
                number_of_replicas: 1
